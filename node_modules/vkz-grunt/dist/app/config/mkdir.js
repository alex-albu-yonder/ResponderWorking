module.exports = function (grunt) {
    return {
        addAndroidReleaseFolder: {
            options: {
                create: ['dist/android/<%=  grunt.task.current.args[0] %>']
            }
        },
        addIOSReleaseFolder: {
            options: {
                create: ['dist/ios/<%=  grunt.task.current.args[0] %>']
            }
        }
    };
};
