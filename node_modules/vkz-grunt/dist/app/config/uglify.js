module.exports = {
    options: {
        // the banner is inserted at the top of the output
        banner: '/*! <%= pkg.name %> <%= grunt.template.today("dd-mm-yyyy") %> */\n'
    },
    release: {
        files: [
            {
                expand: true,
                cwd: 'www/versions/<%= grunt.task.current.args[0] %>/js/',
                src: '**/*.js',
                dest: 'www/versions/<%= grunt.task.current.args[0] %>/js/'
            }
        ]
    }
};
