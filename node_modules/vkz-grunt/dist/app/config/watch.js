module.exports = function(grunt) {
    return {
        all: {
            files: ['src/**/*.js', 'src/**/*.css', 'src/**/*.html', '!src/**/bower_components/**', 'src/**/bower_components/vkz-*/**/*'],
            tasks: [
                'syncSelection:<%= grunt.task.current.args[1] %>',
                'htmlbuildSelection:<%= grunt.task.current.args[1] %>',
                'postcssSelection:<%= grunt.task.current.args[1] %>'
            ]
        },
        allSlow: {
          options: {
            // Options for the watch task go here
            interval: 2000
          },
          files: ['src/**/*.js', 'src/**/*.css', 'src/**/*.html', '!src/**/bower_components/**', 'src/**/bower_components/vkz-*/**/*'],
          tasks: [
              'syncSelection:<%= grunt.task.current.args[1] %>',
              'htmlbuildSelection:<%= grunt.task.current.args[1] %>',
              'postcssSelection:<%= grunt.task.current.args[1] %>'
          ]
        }
    };
};
