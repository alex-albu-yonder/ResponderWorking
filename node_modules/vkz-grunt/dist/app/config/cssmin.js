module.exports = {
    release: {
        files: {
            'www/versions/<%= grunt.task.current.args[0] %>/css/main.css': [
                'src/versions/<%= grunt.task.current.args[0] %>/css/*.css'
            ],
            'www/versions/<%= grunt.task.current.args[0] %>/css/libs.css': [
                'src/versions/<%= grunt.task.current.args[0] %>/bower_components/**/*.css'
            ]
        },
        options: {
            aggressiveMerging: false,
            shorthandCompacting: false
        }
    }
};
