module.exports = {
    options: {
        encoding: 'utf8',
        algorithm: 'md5',
        length: 20
    },
    release: {
        // filerev:release hashes(md5) all assets (images, js and css )
        // in dist directory
        files: [{
            src: [
                'www/img/**/*.{png,gif,jpg,svg}',
                'www/js/**/*.js',
                'www/css/**/*.css'
            ]
        }]
    }
};
