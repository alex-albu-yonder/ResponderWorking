module.exports = function (grunt) {

    grunt.registerTask('coverageThreshold', 'Checks the thresholds on the istanbul report and then chooses what to do', function () {
        var filepath = 'js.coverage/report-json/coverage-final.json';
        var jsonObject = grunt.file.readJSON(filepath);

        console.log(jsonObject);

        var istanbul = new require('istanbul').Collector();
        console.log(istanbul);
    });
};
