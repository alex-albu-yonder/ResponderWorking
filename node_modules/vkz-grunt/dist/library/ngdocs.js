module.exports = function (grunt) {
    return {
        options: {
            dest: 'docs',
            scripts: ['angular.js'],
            html5Mode: true,
            startPage: '/api',
            title: '<%= grunt.config.data.pkg.name %>'
        },
        api: {
            src: ['src/**/*.js'],
            title: '<%= grunt.config.data.pkg.name %> API Documentation'
        }
    };
};
