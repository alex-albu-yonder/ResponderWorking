module.exports = function (grunt) {
    var path = require('path');

    require('time-grunt')(grunt);

    // config
    require('load-grunt-config')(grunt, {
        init: true,
		configPath: [
			path.join(process.cwd(), 'node_modules/vkz-grunt/dist/web'),
			path.join(process.cwd(), 'node_modules/vkz-grunt/dist/web/config'),
			path.join(process.cwd(), 'node_modules/vkz-grunt/dist/web/tasks'),
			path.join(process.cwd(), 'node_modules/vkz-grunt/dist/shared')
		],
        jitGrunt: {
            staticMappings: {
                htmlbuild: 'grunt-html-build',
                ngtemplates: 'grunt-angular-templates',
                ngannotate: 'grunt-ng-annotate'
            }
        },
        data: {
            pkg: grunt.file.readJSON('package.json'),
            config: grunt.file.readJSON('config/config.json')
        }
    });
};
